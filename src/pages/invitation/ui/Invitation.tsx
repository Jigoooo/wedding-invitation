import { Stack, Typography } from '@mui/joy';

export function Invitation() {
  return (
    <Stack
      sx={{ height: '100%', minWidth: 360, maxWidth: 420, width: '100%', alignItems: 'center' }}
    >
      <Typography>Invitation</Typography>
    </Stack>
  );
}
